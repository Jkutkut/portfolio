import type { RepoIdentifier } from "./data/types";

export const TITLE: string = 'Jkutkut';

// External data:
export const repos: RepoIdentifier[] = [
  { type: 'github', usr: 'jkutkut', repoName: 'osmia' },
];
