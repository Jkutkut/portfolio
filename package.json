{
  "name": "portfolio",
  "type": "module",
  "version": "0.6.6",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "updateData": "npx tsx ./src/data/updateData.ts",
    "cleanData": "rm -rf ./data",
    "prepareCommit": "${EDITOR} package.json && npm i --package-lock-only && git add -N . && git add -p"
  },
  "dependencies": {
    "astro": "^5.9.2",
    "marked": "^15.0.12"
  }
}
